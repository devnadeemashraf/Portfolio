export const sendSiteInteractionNotification = async (message: string) => {
  // const resp = await fetch("http://localhost:3000/api/geo", {
  //   method: "GET",
  // });

  // console.log(resp);

  return;
};
