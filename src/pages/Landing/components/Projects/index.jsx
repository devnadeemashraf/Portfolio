import Container from "~/src/components/shared/container";

const Projects = () => {
  return (
    <Container type="section" id="projects">
      <h1>Hey I am Projects</h1>
      {/* 
      Horizontal Scroll
      [Card] [Card] [Card] ->
      */}
    </Container>
  );
};

export default Projects;
